<h1>Welcome to your task's list !</h1>

<ul>
  <% @tasks.each do |task| %>
  <li><%= task.title %> - <%= task.details %> - <%= task.completed %> <%= link_to "show the task", task_path(task) %>
</li>
  <% end %>
</ul>
